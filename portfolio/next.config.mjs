/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: "/portfolio",
    output: 'export' // Added Export so to generate Static HTML CSS so portfolio can be run on Github Pages
};

export default nextConfig;
